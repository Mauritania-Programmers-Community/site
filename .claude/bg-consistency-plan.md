# Background Consistency Plan

## Proposed Pattern (Alternating Clean/Subtle)

1. **Hero** - `bg-background` + mesh (keep as is)
2. **Stats** - `bg-muted/30` + subtle blobs (simplify, remove dots)
3. **Features** - `bg-background` + subtle blobs (clean)
4. **Community Spotlight** - `bg-muted/30` + subtle blobs (remove geometric)
5. **Community** - `bg-background` + subtle blobs (clean)
6. **FAQ** - `bg-muted/30` + subtle blobs (keep)
7. **CTA** - **Bold green gradient** (keep - perfect ending)

## Changes Needed:

### Stats Section
- Change from gradient overlay to `bg-muted/30`
- Remove dotted pattern
- Keep animated blobs (subtle)

### Features Section
- Change from `bg-muted/30` to `bg-background`
- Add subtle animated blobs

### Community Spotlight
- Change from `bg-background pattern-geometric` to `bg-muted/30`
- Remove geometric pattern
- Add subtle blobs

### Community Section
- Change to `bg-background`
- Keep blobs

## Result:
Clean alternating: white → tint → white → tint → white → tint → GREEN
All sections have subtle animated blobs for consistency, no competing patterns.
